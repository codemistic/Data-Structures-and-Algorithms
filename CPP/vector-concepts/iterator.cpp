#include<bits/stdc++.h>
#define int long long
#define ll long long
#define pb push_back
#define fr(i,a,b) for(int i=a;i<b;i++)
#define mod 1000000007
#define read(x) int x;cin>>x


using namespace std;
signed main(){
	// std::vector<int> v;
	// v={12,13,14,15,16};
	// fr(i,0,v.size())
	// cout<<v[i]<<" ";
	// cout<<endl;
	// vector<int>::iterator it;
	// for(it=v.begin();it!=(v.end()-1);it++)
	// 	cout<<*(it+1)<<endl;

	// std::vector<pair<int,int>> v;
	// v={{12,13},{44,55},{55,66},{34,43}};
	// fr(i,0,v.size()) 
	// cout<<v[i].first<<","<<v[i].second<<endl;
	// cout<<endl<<endl;
	// vector<pair<int,int>>::iterator it;
	// for(it=v.begin();it!=v.end();it++){
	// 	cout<<(*it).first<<","<<(*it).second<<endl;
	// }
	// cout<<endl<<endl;



	
	// //by using "AUTO" we can write iterator in shortcut
	// for(auto it=v.begin();it!=v.end();it++)
	// 	cout<<it->first<<", "<<it->second<<endl;



	int arr[9];
	for(auto &x:arr)
		cin>>x;
	for(auto z:arr)
		cout<<z<<endl;


	return 0;
}
